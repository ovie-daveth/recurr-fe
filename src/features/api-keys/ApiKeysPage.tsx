import { PageHeader } from "../../components/ui/PageHeader";

export function ApiKeysPage() {
  return <PageHeader title="API keys" description="Create test and live keys, copy new keys, and revoke old keys." />;
}
